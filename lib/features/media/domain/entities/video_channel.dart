class VideoChannel {
  final String name;
  final String url;
  final String description;

  const VideoChannel({
    required this.name,
    required this.url,
    required this.description,
  });
}
